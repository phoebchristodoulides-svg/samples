name: Build Android App Bundle

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'

    - name: Install dependencies
      run: |
        cd animations
        flutter pub get

    - name: Build Digital App Bundle (.aab)
      run: |
        cd animations
        flutter build appbundle --release

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-bundle
        path: animations/build/app/outputs/bundle/release/app-release.aab
